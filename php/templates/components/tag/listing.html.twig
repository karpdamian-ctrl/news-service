{% set tag = listing.tag %}

<section class="mb-4">
    <h1 class="display-6 fw-bold mb-2">
        {% if tag %}
            Tag: #{{ tag.name }}
        {% else %}
            Tag nie znaleziony
        {% endif %}
    </h1>
    {% if tag %}
        <p class="text-secondary mb-0">Artykuły przypisane do tagu „{{ tag.name }}”.</p>
    {% endif %}
</section>

<section class="row g-4">
    {% for article in listing.items %}
        <div class="col-12 col-lg-6">
            <article class="card news-card h-100">
                <div class="card-body">
                    {% include 'components/shared/article-tile-content.html.twig' with {
                        article: article,
                        title_tag: 'h2',
                        title_class: 'listing-title mb-2',
                        image_link_class: 'd-block',
                        image_class: 'card-img-top',
                        image_style: 'height: 220px; object-fit: cover;',
                        description_class: 'text-secondary mb-3',
                        meta_class: 'meta-line mb-3'
                    } only %}
                </div>
            </article>
        </div>
    {% else %}
        {% if tag %}
            <div class="col-12">
                <div class="alert alert-secondary mb-0">Brak artykułów dla tego tagu.</div>
            </div>
        {% endif %}
    {% endfor %}
</section>
