<footer class="site-footer py-4">
    <div class="container d-flex flex-column flex-md-row gap-3 justify-content-between align-items-md-center">
        <div>
            <strong>{{ footer.site_name }}</strong>
            <div class="small opacity-75">Wiadomości, które mają znaczenie.</div>
            <div class="small opacity-75">Autor: {{ footer.author }}</div>
        </div>
        <div class="small opacity-75">© {{ footer.year }}</div>
    </div>
</footer>
